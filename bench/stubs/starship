#!/bin/sh
# Stand-in for starship: emits an init block of representative size, then exits.
# The process spawn is the point — it is what `eval "\$(starship init zsh)"` costs.
exec "$(dirname "$0")/gen.sh" starship 20
