#!/bin/sh
# Stand-in for fzf: emits an init block of representative size, then exits.
# The process spawn is the point — it is what `eval "\$(fzf init zsh)"` costs.
exec "$(dirname "$0")/gen.sh" fzf 20
